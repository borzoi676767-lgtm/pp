{%- comment -%} Stone/parchment placeholder — never a broken image. {%- endcomment -%}
<div class="mc-placeholder" role="img" aria-label="{{ label | default: 'Milites Christi' | escape }}">
  {% render 'icon', name: 'chi-rho', size: 96 %}
</div>
